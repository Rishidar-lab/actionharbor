{
  "name": "@actionharbor/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure, framework-independent domain logic. No I/O, no adapters, no clock reads except through the Clock port.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@actionharbor/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}

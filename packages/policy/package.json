{
  "name": "@actionharbor/policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure, deterministic policy decision point (PDP). No I/O, no clock reads, no adapter calls — a function of its arguments only.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@actionharbor/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}

{
  "name": "@actionharbor/ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The append-only, hash-chained audit ledger. Server-authored events only; no public update/delete API; integrity is verified by recomputing the chain, not trusted from storage.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@actionharbor/contracts": "workspace:*",
    "@actionharbor/domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
